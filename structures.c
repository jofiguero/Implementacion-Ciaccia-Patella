#include <stdio.h>
#include <stdlib.h>

typedef struct{
    Node *root;
} Tree;

typedef struct{
    Entry *List;
    int count;
} Node;

typedef struct {
    double p;
    double cr;
    Node *a;
} Entry;

int main(){
    printf(siezeof(Entry))
    return 0;
}